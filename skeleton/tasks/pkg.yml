---
- name: Install required packages
  pkgng:
    name:
      - bash
      - bash-completion
      - curl
      - coreutils
      - diffutils
      - gawk
      - git
      - gmake
      - gnugrep
      - gnuls
      - gnupg
      - gsed
      - gtar
      - mc
      - nmap
      - psmisc
      - python3
      - python311
      - rsync
      - screen
      - util-linux
      - vim
      - wget
    state: present
  tags: ['pkg']

#- name: Add dports collection
#  git:
#    repo: https://github.com/DragonFlyBSD/DPorts
#    dest: /usr/dports
#    depth: 1
#  tags: ['ports']
